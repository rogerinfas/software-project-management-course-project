{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "description": "Espacio reservado para el frontend definitivo. El prototipo UI actual está en el paquete `prototype`.",
  "scripts": {
    "lint": "true",
    "build": "true"
  }
}
